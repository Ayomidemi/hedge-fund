APP_NAME=Hedge Fund API
APP_ENV=development
DEBUG=true

API_HOST=0.0.0.0
API_PORT=8000

# Comma-separated origins for CORS
CORS_ORIGINS=http://localhost:3000

# Local Docker Postgres:
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/hedge_fund

# Supabase Postgres pooler also works. The backend prefers this value when set.
# Raw passwords are accepted and normalized by the backend.
# HF_SUPABASE_DATABASE_URL=postgresql://postgres.[project-ref]:[password]@[pooler-host]:6543/postgres

# Supabase Auth (required to protect API routes)
# HF_SUPABASE_URL=https://your-project-ref.supabase.co
# HF_SUPABASE_JWT_SECRET=your-supabase-jwt-secret
# Modern Supabase projects also verify ES256 tokens via JWKS when HF_SUPABASE_URL is set.

# Background cadences
HF_PRICE_REFRESH_INTERVAL_SECONDS=300
HF_NEWS_POLL_INTERVAL_SECONDS=60
HF_NEWS_POLL_JURISDICTIONS=US
HF_NEWS_TICKER_REFRESH_TTL_SECONDS=1800
HF_NEWS_RETENTION_DAYS=50

# Market data provider credentials. Tiingo remains the primary listed-market
# source. Fixed-income sources are used by the provider ladder as each feed is
# connected and licensed.
# HF_TIINGO_API_KEY=
# HF_FMP_API_KEY=
# HF_POLYGON_API_KEY=
# HF_NGNMARKET_API_KEY=
# HF_ALPACA_BROKER_API_KEY=
# HF_ALPACA_BROKER_API_SECRET=
# HF_FMDQ_API_KEY=
# HF_FRED_API_KEY=
